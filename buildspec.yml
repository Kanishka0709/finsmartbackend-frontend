version: 0.2

phases:
  install:
    commands:
      - apt-get update
      - apt-get install -y maven

  build:
    commands:
      - echo "Building project..."
      - mvn clean package

artifacts:
  files:
    - target/*.jar
    - appspec.yaml
    - scripts/*
